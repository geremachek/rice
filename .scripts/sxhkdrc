# scrot
super + shift + s
	scrot

# enable external monitor
super + shift + e
	/home/gmk/.scripts/enablemon

# audio
XF86Audio{RaiseVolume,LowerVolume}
	amixer set Master {5%+,5%-}

XF86AudioMute
	amixer set Master toggle

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd
